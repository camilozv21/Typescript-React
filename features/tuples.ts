const drink = {
    color: 'brown',
    carbonated: true,
    sugar: 40
};

type Drink = [string, boolean, number];

const pepsi: Drink = ['brown', true, 40];
const sprite: Drink = ['clear', true, 80]
const tes: Drink = ['brown', false, 0]

const carSpcs: [number, number] = [400, 3354];

const carStats = {
    hoserpower: 400,
    weight: 3354
};
